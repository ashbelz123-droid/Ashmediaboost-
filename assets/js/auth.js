document.addEventListener('DOMContentLoaded',()=>{const reg=document.getElementById('registerForm');const login=document.getElementById('loginForm');const msg=document.getElementById('formMessage');const setMsg=(text,type='')=>{if(msg){msg.textContent=text;msg.className=`form-message ${type}`}};if(reg)reg.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(reg),p=String(f.get('password')||'');if(p.length<8)return setMsg('Password must contain at least 8 characters.','error');setMsg('Account UI is ready. Connect Supabase Auth before enabling real registration.','success')});if(login)login.addEventListener('submit',e=>{e.preventDefault();setMsg('Login UI is ready. Connect Supabase Auth before enabling real login.','success')})});